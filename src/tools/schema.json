[
  {
    "type": "function",
    "function": {
      "name": "calculate",
      "description": "Computes the arithmetic expression given by user",
      "parameters": {
        "type": "object",
        "properties": {
          "expression": {
            "type": "string",
            "description": "Arithmetic expression e.g. 2 + 3 * 5"
          }
        },
        "required": ["expression"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "getCurrentTime",
      "description": "gets the current time",
      "parameters": {
        "type": "object",
        "properties": {}
      }
    }
  }
]
